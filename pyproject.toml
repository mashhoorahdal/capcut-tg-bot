[project]
name = "capcut-tg-bot"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "python-telegram-bot==20.7",
    "python-dotenv==1.0.1",
]

[dependency-groups]
dev = [
    "pytest==8.1.1",
    "pytest-asyncio==0.23.6",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
